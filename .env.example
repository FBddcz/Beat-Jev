PORT=8791
# Online deployments should use HOST=0.0.0.0 and list the public host here.
HOST=127.0.0.1
ALLOWED_HOSTS=127.0.0.1,localhost,[::1]
# Optional: keys can also be set in the game's connection dialog.
TYPESAFE_API_KEY=
JEV_MODEL=jev-latest
